# Frontend environment variables (Vite exposes only VITE_* keys to the browser).
# Copy to "client/.env" for local development and set the same key in Vercel.

# Firebase Web API key, used ONLY for Google sign-in (auth), not for storage.
# Image storage is handled by Cloudinary via the backend.
VITE_FIREBASE_API_KEY=
